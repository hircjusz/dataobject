using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace SoftwareMind.Base
{
    /*public class AuthInfoBase : IAuthInfo
    {
        public long LoggedUserId { get; protected set; }
        public long EffectiveUserId { get; protected set; }
        public long ProfileId { get; protected set; }
        public string UserApplication { get; protected set; }
        public string UserLanguage { get; protected set; }

        public AuthInfoBase()
        {
            LoggedUserId = -1;
            EffectiveUserId = -1;
            ProfileId = -1;
            UserApplication = String.Empty;
            UserLanguage = String.Empty;
        }

        public AuthInfoBase(long loggedUserId, long effectiveUserId, long profileId, string userApplication, string userLanguage)
        {
            LoggedUserId = loggedUserId;
            EffectiveUserId = effectiveUserId;
            ProfileId = profileId;
            UserApplication = userApplication;
            UserLanguage = userLanguage;
        }
    }*/
}
